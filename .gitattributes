# Auto normalize line endings
* text=auto

# Shell scripts and binaries must have LF
*.sh text eol=lf
*.bash text eol=lf
.bin/ text eol=lf

# Ensure node_modules/.bin/* scripts use LF
node_modules/.bin/* text eol=lf
